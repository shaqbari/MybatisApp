<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="app.emp.Emp"> -->
<mapper namespace="Emp">
	<sql id="basic">
		select * from emp
	</sql>
	<select id="selectAll" resultType="Emp">
		<include refid="basic"/>
	</select>
	<select id="select" resultType="Emp" parameterType="int">
		<!-- select * from emp where emp_no=? -->
		<include refid="basic"/> where empno=#{empno}
	</select>
</mapper>

<!--ibatis, mybatis에서는 쿼리문을 자바코드안에 두지 않는다 왜? 유지보수성을 높이기 위해. 
	namespace는 유일성 확보, resultType은 반환형
	둘이 길어지므로 config에서 allias 설정하면 짧게 할 수 있다.
	*는 성능이 떨어지기때문에 원래 column명을 넣는 것이 원칙이다. 이를 유지보수하기 쉽기위해 공통적인 부분을 sql태그에 만들어 쓰자
-->